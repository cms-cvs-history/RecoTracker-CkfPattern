process tracking = 
{
  ### keep the logging output to a nice level ###
  include "FWCore/MessageService/data/MessageLogger.cfi"
  replace MessageLogger.cout.default.limit = 100000
	 
  source = PoolSource 
  { 
     untracked vstring fileNames = {'rfio:/castor/cern.ch/user/l/lenzip/StandardTracking/hits_133.root'}
     untracked int32 maxEvents = -1
  }

  service = SimpleMemoryCheck {
    untracked int32 ignoreTotal = 1 # default is one
  }
	
  ### SeedGenerator module ###
  include "RecoTracker/TkSeedGenerator/data/GlobalMixedSeeds.cff"

  # KFTrackCandidateMaker
  include "RecoTracker/CkfPattern/data/CkfTrackCandidates.cff"

  # CTF track fit with material
  include "RecoTracker/TrackProducer/data/CTFFinalFitWithMaterial.cff"

  module out = PoolOutputModule 
  {
     untracked string fileName = 'tracks_mixedSeeded_133modified.root'
     untracked vstring outputCommands = 
      {
        "keep *"
# 	"drop *_siPixelDigis_*_*",
#	"drop *_siStripDigis_*_*"

 #      "drop *_TrackCandidate_*_*"
 #	"drop *_g4SimHits_*_*",
 # 	"drop *_siPixelClusters_*_*",
 #	"drop *_siStripClusters_*_*",
 #      "drop *_RecHit_*_*",

       }
  }
  
  path p = {globalMixedSeeds ,
            ckfTrackCandidates,
            ctfWithMaterialTracks
  }

  endpath e = {out}
}
